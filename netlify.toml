[build]
  command = "curl -fsSL https://github.com/gohugoio/hugo/releases/download/v0.128.0/hugo_extended_0.128.0_linux-amd64.tar.gz | tar -C /tmp -xz && /tmp/hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.128.0"
